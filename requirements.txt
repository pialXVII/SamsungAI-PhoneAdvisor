# Samsung Phone Query and Review System
# Tested on Python 3.13 (Windows 11).

# --- Scraping -------------------------------------------------------------
requests>=2.32.0
beautifulsoup4>=4.13.0
lxml>=5.0.0

# --- Database -------------------------------------------------------------
SQLAlchemy>=2.0.30
PyMySQL>=1.1.0          # MySQL / MariaDB driver
cryptography>=42.0.0    # required by PyMySQL for caching_sha2_password auth
# psycopg2-binary>=2.9.9  # uncomment for PostgreSQL

# --- RAG / open-source models --------------------------------------------
torch>=2.2.0
transformers>=4.44.0
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0
numpy>=1.26.0

# --- Multi-agent orchestration -------------------------------------------
# CrewAI drives the agent crews. It is a large install (~70 packages); if it is
# absent the system automatically falls back to the built-in orchestrator in
# src/agents/base.py, so the project still runs without it.
crewai>=1.0.0

# --- API ------------------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-dotenv>=1.0.0
